# 2 核 2G 云服务器完整落地方案（修订版，适配 JDK11 + Kafka3.5.1 KRaft + root 账号）

## 硬件约束说明

云服务器：**2 核 2G**，内存为最大瓶颈，无法运行 Hadoop、YARN、Zookeeper。 **整体架构**：Kafka 3.x KRaft（无 ZK）+ Spark Local [1] 本地模式 + Python3.8 + Flask‑SocketIO + ECharts

> 所有组件单机器运行；牺牲分布式能力，完整保留项目业务逻辑：传感器模拟、流处理 5s 滑动窗口、消息队列、SocketIO 实时推送、大屏可视化。

数据流： `Python模拟传感器数据 → Kafka(agri_sensor) → Spark Structured Streaming(local[1]) 5s滑动窗口聚合 → Kafka(agri_result) → Flask消费结果SocketIO推送 → ECharts前端大屏`

## 一、环境安装与关键调参

### 1. 基础环境

1. **JDK11（推荐）**，Spark‑3.2.x 对 JDK11 兼容性好，规避 JDK17 警告异常。
2. Python3.8+

```
pip3 install kafka-python flask-socketio eventlet
```

> eventlet：Flask‑SocketIO 依赖，用于 websocket 长连接。

1. Spark‑3.2.x 预编译包，**无需启动 Spark 集群服务**，只用 local 本地模式。
2. Kafka‑3.x 使用 **KRaft 模式（抛弃 Zookeeper）**

#### Kafka 关键调参（适配 2G 内存）

> ⚠️重要：Kafka3.x KRaft 版本不存在`config/kraft/jvm.options`配置文件，堆内存通过环境变量`KAFKA_HEAP_OPTS`控制。 **每次新开终端启动 Kafka 前必须执行下面命令**：

```
export KAFKA_HEAP_OPTS="-Xms128m -Xmx256m"
```

生成 KRaft 集群 id，格式化存储【**仅首次部署执行一次，重启服务器禁止重复执行 format，会清空全部 Kafka 数据**】，启动单节点 kraft broker，创建两个 topic

```
# 生成集群ID
KAFKA_CLUSTER_ID="$(bin/kafka-storage.sh random-uuid)"
bin/kafka-storage.sh format -t $KAFKA_CLUSTER_ID -c config/kraft/server.properties

#后台启动kafka
nohup bin/kafka-server-start.sh config/kraft/server.properties > kafka.log 2>&1 &

# 创建topic
bin/kafka-topics.sh --create --topic agri_sensor --bootstrap-server 127.0.0.1:9092 --partitions 1 --replication-factor 1
bin/kafka-topics.sh --create --topic agri_result --bootstrap-server 127.0.0.1:9092 --partitions 1 --replication-factor 1
```

> partitions=1，replication‑factor=1，单机环境减少资源开销。

> root 账号提前创建 Spark checkpoint 目录：

```
mkdir -p /root/agri_checkpoint
```

## 二、代码总共有 4 部分

1. `sensor_producer.py`：模拟大棚传感器，生产数据发送 Kafka `agri_sensor`
2. `spark_stream.py`：PySpark Structured Streaming，读取 Kafka，5 秒滑动窗口聚合，结果写回`agri_result`
3. `flask_server.py`：消费`agri_result`，通过 SocketIO 向前端浏览器推送 JSON 数据
4. `index.html`：前端大屏，Socket.io 接收数据，ECharts 绘制实时折线图

### ① sensor_producer.py 传感器模拟生产者

> 控制发送频率，2 秒一条，避免压爆 2 核 2G 机器

```
import json
import time
import random
from kafka import KafkaProducer

producer = KafkaProducer(
    bootstrap_servers=['127.0.0.1:9092'],
    value_serializer=lambda v: json.dumps(v).encode('utf-8')
)

if __name__ == "__main__":
    while True:
        #模拟大棚传感器指标
        data = {
            "air_temp": round(random.uniform(18,32),2),       #空气温度
            "air_humidity": round(random.uniform(40,70),2),    #空气湿度
            "soil_humidity": round(random.uniform(30,60),2),   #土壤湿度
            "light": round(random.uniform(2000,5000),2),       #光照强度
            "timestamp": int(time.time()*1000)
        }
        producer.send("agri_sensor", value=data)
        print(f"发送传感器数据:{data}")
        time.sleep(2) #❗2秒发送1条，降低流量，适配低配机器
```

### ② spark_stream.py PySpark 实时流计算程序

> ⚠️配置：`local[1]`单线程，driver 内存 512M；checkpoint 写**root 账号服务器本地磁盘目录/root/agri_checkpoint，不要写 HDFS，不要写 /home/ubuntu 路径**。 业务逻辑：读取 agri_sensor，JSON 解析；**5s 滑动窗口，滑动步长 1s**，求各指标均值；输出 JSON 写入 agri_result。

```
from pyspark.sql import SparkSession
from pyspark.sql.functions import from_json, col, window, avg

spark = SparkSession.builder \
    .appName("agri_realtime") \
    .master("local[1]") \
    .config("spark.driver.memory","512m") \
    .getOrCreate()

spark.sparkContext.setLogLevel("WARN")

#读取kafka原始数据流
df_raw = spark.readStream \
    .format("kafka") \
    .option("kafka.bootstrap.servers","127.0.0.1:9092") \
    .option("subscribe","agri_sensor") \
    .option("startingOffsets","latest") \
    .load()

#定义JSON schema，解析传感器数据
schema = "air_temp double, air_humidity double, soil_humidity double, light double, timestamp long"

df_parse = df_raw.select(from_json(col("value").cast("string"), schema).alias("data")).select("data.*")

#转换时间戳为timestamp类型用于窗口计算
df_parse = df_parse.withColumn("ts", col("timestamp")/1000).selectExpr("to_timestamp(ts) as event_time","*")

#====核心：5秒滑动窗口，滑动1秒，聚合求均值====
df_window = df_parse.groupBy(
    window(col("event_time"), windowDuration="5 seconds", slideDuration="1 second")
).agg(
    avg("air_temp").alias("air_temp_avg"),
    avg("air_humidity").alias("air_humidity_avg"),
    avg("soil_humidity").alias("soil_humidity_avg"),
    avg("light").alias("light_avg")
)

#整理输出字段，把窗口结束时间作为时间戳输出
df_out = df_window.selectExpr(
    "window.end as ts",
    "air_temp_avg",
    "air_humidity_avg",
    "soil_humidity_avg",
    "light_avg"
).selectExpr("to_json(struct(*)) as value")

#输出结果写回kafka agri_result主题
query = df_out.writeStream \
    .format("kafka") \
    .option("kafka.bootstrap.servers","127.0.0.1:9092") \
    .option("topic","agri_result") \
    .option("checkpointLocation","/root/agri_checkpoint") #❗root账号使用该路径，必须提前手动创建文件夹
    .outputMode("update") \
    .start()

query.awaitTermination()
```

> 后台启动命令

```
nohup spark-submit --master local[1] spark_stream.py > spark.log 2>&1 &
```

### ③ flask_server.py：消费 kafka 结果，SocketIO 实时向前端推送

> 后台线程消费 kafka `agri_result`；收到计算结果，通过`socketio.emit`推送给浏览器。单线程运行，减少内存开销。

```
import json
from kafka import KafkaConsumer
from flask import Flask, render_template
from flask_socketio import SocketIO
import threading

app = Flask(__name__)
app.config['SECRET_KEY'] = 'secret'
socketio = SocketIO(app, cors_allowed_origins="*")

#kafka消费者，单独后台线程运行
def kafka_consumer_task():
    consumer = KafkaConsumer(
        'agri_result',
        bootstrap_servers=['127.0.0.1:9092'],
        auto_offset_reset='latest',
        value_deserializer=lambda x: json.loads(x.decode('utf-8'))
    )
    for msg in consumer:
        data = msg.value
        print(f"收到计算结果:{data}")
        socketio.emit("agri_update", data) #推送消息给前端

@app.route('/')
def index():
    return render_template("index.html")

if __name__ == "__main__":
    #启动消费线程
    t = threading.Thread(target=kafka_consumer_task, daemon=True)
    t.start()
    socketio.run(app, host="0.0.0.0", port=5000, debug=False)
```

> 目录结构：同目录新建`templates`文件夹，把`index.html`放入 templates 下。 后台启动 flask：

```
nohup python3 flask_server.py > flask.log 2>&1 &
```

### ④ templates/index.html 前端大屏 ECharts 可视化

> Socket.io 接收后端推送`agri_update`消息；动态追加数据点，最多保存 30 个点自动滚动。

```
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>智慧农业大棚实时监测平台</title>
    <script src="https://cdn.jsdelivr.net/npm/echarts@5.4/dist/echarts.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/socket.io@4.7/dist/socket.io.min.js"></script>
    <style>
        body{background:#0f241c;color:#fff;padding:20px;}
        .card-group{display:flex;gap:20px;margin-bottom:20px;}
        .card{background:#1a3b2e;padding:15px;border-radius:8px;width:200px;}
        #main{height:450px;background:#1a3b2e;border-radius:8px;}
    </style>
</head>
<body>
<h2>智慧农业大棚环境实时监测分析平台</h2>
<div class="card-group">
    <div class="card">空气温度(℃)<div id="val_temp">--</div></div>
    <div class="card">空气湿度(%)<div id="val_hum">--</div></div>
    <div class="card">土壤湿度(%)<div id="val_soil">--</div></div>
    <div class="card">光照强度(lux)<div id="val_light">--</div></div>
</div>
<div id="main"></div>

<script>
    //连接后端socketio服务
    const socket = io();
    const chartDom = document.getElementById('main');
    const myChart = echarts.init(chartDom);

    const option={
        tooltip:{},
        legend:{data:["空气温度","土壤湿度"]},
        xAxis:{type:'category',data:[]},
        yAxis:{type:'value'},
        series:[
            {name:"空气温度",type:"line",data:[]},
            {name:"土壤湿度",type:"line",data:[]}
        ]
    };
    myChart.setOption(option);

    const maxPoint=30; //最多保存30个数据点

    socket.on("agri_update",function(data){
        //更新顶部卡片数值
        document.getElementById("val_temp").innerText=data.air_temp_avg.toFixed(2);
        document.getElementById("val_hum").innerText=data.air_humidity_avg.toFixed(2);
        document.getElementById("val_soil").innerText=data.soil_humidity_avg.toFixed(2);
        document.getElementById("val_light").innerText=data.light_avg.toFixed(0);

        //时间格式化
        let timeStr=new Date(data.ts).toLocaleTimeString();

        //向图表追加数据
        let xData=myChart.getOption().xAxis[0].data;
        let tempData=myChart.getOption().series[0].data;
        let soilData=myChart.getOption().series[1].data;

        xData.push(timeStr);
        tempData.push(data.air_temp_avg);
        soilData.push(data.soil_humidity_avg);

        //超过30点移除头部旧数据
        if(xData.length>maxPoint){
            xData.shift();
            tempData.shift();
            soilData.shift();
        }
        myChart.setOption({xAxis:{data:xData},series:[{data:tempData},{data:soilData}]});
    });
</script>
</body>
</html>
```

## 三、完整启动顺序（2 核 2G 云服务器 root 账号）

> ⚠️重要：新开终端，进入 kafka 目录，**先执行 export KAFKA_HEAP_OPTS="-Xms128m -Xmx256m"** 全部使用 nohup 后台运行；云服务器安全组放行端口：**9092(Kafka)、5000(Flask)** 每启动一个组件，等待 5‑10 秒，让服务初始化完毕，**禁止一次性全部粘贴后台执行**。

1. 启动 Kafka KRaft

```
nohup bin/kafka-server-start.sh config/kraft/server.properties > kafka.log 2>&1 &
```

> 等待 5‑10 秒，使用`jps`确认 Kafka 进程存在。

1. 提交 Spark 流计算任务

```
nohup spark-submit --master local[1] spark_stream.py > spark.log 2>&1 &
```

> 等待 5 秒

1. 启动 Flask‑SocketIO 后端服务

```
nohup python3 flask_server.py > flask.log 2>&1 &
```

> 等待 2‑3 秒

1. 启动传感器模拟生产脚本

```
nohup python3 sensor_producer.py > producer.log 2>&1 &
```

1. 浏览器访问：`http://你的云服务器公网IP:5000` 即可看到实时大屏。

> ✅停止顺序（反向）：停止 sensor_producer → 停止 flask_server → 停止 spark 任务 → 最后停止 Kafka。

## 四、故障排查 & 2C2G 机器注意事项

1. 内存不足进程被杀 (OOM)
   - 新开终端启动 Kafka 务必执行`export KAFKA_HEAP_OPTS="-Xms128m -Xmx256m"`；Spark driver 内存 512M；使用`local[1]`，**严禁 local [\*] 打满 CPU 内存**。
   - 传感器脚本`sleep(2)`不要调小，不要高频发送。
2. 端口访问不通：检查云服务器安全组入方向规则、服务器本机防火墙。
3. checkpoint 目录必须提前手动创建：`mkdir -p /root/agri_checkpoint`，注意账号权限。
4. Kafka 的`kafka‑storage.sh format`只允许执行一次，重复执行会清空所有 topic 与消息。
5. 查看日志定位报错：

```
tail -f kafka.log
tail -f spark.log
tail -f flask.log
tail -f producer.log
```

1. 停止服务：查询 PID 后执行`kill -9 进程号`。

## 五、实训报告硬件约束描述（可直接复制）

> 本项目部署环境为 2 核 2G 云服务器，硬件资源有限，无法部署 Hadoop、YARN、Zookeeper 组件。 因此采用轻量化实现方案：Kafka 3.x KRaft 模式去除 Zookeeper 依赖；Spark 采用`local[1]`本地模式完成 Structured Streaming 流计算，Checkpoint 存储服务器本地磁盘。 完整复现传感器数据模拟、Kafka 消息队列传输、5 秒滑动窗口实时聚合计算、SocketIO 服务端主动推送、ECharts 前端可视化全部业务流程。该方案用于课程功能验证；生产环境可升级服务器配置，引入 Hadoop 生态实现完整伪分布式集群。

## 六、项目目录结构参考

```
project/
├─ sensor_producer.py
├─ spark_stream.py
├─ flask_server.py
└─ templates/
   └─ index.html
```

## 七、补充重要备注

1. Kafka 环境变量`KAFKA_HEAP_OPTS`仅当前 SSH 会话生效，新开终端启动 Kafka，必须重新 export。
2. Spark3.2.x 推荐 JDK11，如果服务器多 JDK 并存，在`spark‑env.sh`配置`JAVA_HOME`指向 JDK11。
3. 修改 Python 代码之后，**必须 kill 旧进程，再重新 nohup 启动，修改才会生效**，程序不会自动热更新。